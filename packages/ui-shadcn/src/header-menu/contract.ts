/**
 * The header column-menu slot contract, owned by `@ranjeetk25/schema-grid-ag-grid`
 * (`SchemaGrid.headerMenu`). Re-exported through the adapter so this package
 * keeps a single ag-grid import point.
 */
export type {
  HeaderMenuActions,
  HeaderMenuColumn,
  HeaderMenuComponent,
  HeaderMenuPinnedState,
  HeaderMenuProps,
  HeaderMenuSortState,
} from "../internal/grid-contracts";
